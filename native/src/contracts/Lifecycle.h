// Generated by contracts/generate.mjs. Do not edit.
#pragma once
#include "rpc/Json.h"
#include <cmath>
#include <cstdint>
#include <optional>
#include <string>
namespace corevideo::contracts {
struct ProtocolVersion {
  int major{};
  int minor{};
};
inline bool validateProtocolVersion(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* major = value.get("major");
  if (!major || !(major->isNumber() && std::floor(major->asNumber()) == major->asNumber() && major->asNumber() >= 1 && major->asNumber() <= 1)) return false;
  const auto* minor = value.get("minor");
  if (!minor || !(minor->isNumber() && std::floor(minor->asNumber()) == minor->asNumber() && minor->asNumber() >= 0 && minor->asNumber() <= 2147483647)) return false;
  return true;
}
inline rpc::Json toJson(const ProtocolVersion& value) {
  rpc::Json::Object result;
  result.emplace("major", value.major);
  result.emplace("minor", value.minor);
  return result;
}
struct OutputLifecycle {
  std::string sessionId{};
  bool desiredActive{};
  std::string state{};
  std::string health{};
  bool finalized{};
  std::optional<std::string> error{};
};
inline bool validateOutputLifecycle(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* sessionId = value.get("sessionId");
  if (!sessionId || !(sessionId->isString() && sessionId->asString().size() >= 1)) return false;
  const auto* desiredActive = value.get("desiredActive");
  if (!desiredActive || !(desiredActive->isBool())) return false;
  const auto* state = value.get("state");
  if (!state || !(state->isString() && (state->asString() == "idle" || state->asString() == "starting" || state->asString() == "live" || state->asString() == "stopping" || state->asString() == "finalizing" || state->asString() == "completed" || state->asString() == "failed" || state->asString() == "interrupted"))) return false;
  const auto* health = value.get("health");
  if (!health || !(health->isString() && (health->asString() == "unknown" || health->asString() == "healthy" || health->asString() == "degraded" || health->asString() == "failed"))) return false;
  const auto* finalized = value.get("finalized");
  if (!finalized || !(finalized->isBool())) return false;
  const auto* error = value.get("error");
  if (error && !(error->isString())) return false;
  return true;
}
inline rpc::Json toJson(const OutputLifecycle& value) {
  rpc::Json::Object result;
  result.emplace("sessionId", value.sessionId);
  result.emplace("desiredActive", value.desiredActive);
  result.emplace("state", value.state);
  result.emplace("health", value.health);
  result.emplace("finalized", value.finalized);
  if (value.error) result.emplace("error", *value.error);
  return result;
}
struct OperationStatus {
  std::string processEpoch{};
  std::string operationId{};
  std::string state{};
  std::optional<std::string> error{};
};
inline bool validateOperationStatus(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* operationId = value.get("operationId");
  if (!operationId || !(operationId->isString() && operationId->asString().size() >= 1)) return false;
  const auto* state = value.get("state");
  if (!state || !(state->isString() && (state->asString() == "accepted" || state->asString() == "running" || state->asString() == "completed" || state->asString() == "failed" || state->asString() == "cancelled"))) return false;
  const auto* error = value.get("error");
  if (error && !(error->isString())) return false;
  return true;
}
inline rpc::Json toJson(const OperationStatus& value) {
  rpc::Json::Object result;
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("operationId", value.operationId);
  result.emplace("state", value.state);
  if (value.error) result.emplace("error", *value.error);
  return result;
}
struct ProtocolFailure {
  std::string code{};
  std::string message{};
};
inline bool validateProtocolFailure(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* code = value.get("code");
  if (!code || !(code->isString() && code->asString().size() >= 1)) return false;
  const auto* message = value.get("message");
  if (!message || !(message->isString() && message->asString().size() >= 1)) return false;
  return true;
}
inline rpc::Json toJson(const ProtocolFailure& value) {
  rpc::Json::Object result;
  result.emplace("code", value.code);
  result.emplace("message", value.message);
  return result;
}
struct EntityIdentity {
  std::string kind{};
  std::string id{};
};
inline bool validateEntityIdentity(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* kind = value.get("kind");
  if (!kind || !(kind->isString() && (kind->asString() == "person" || kind->asString() == "participant" || kind->asString() == "source" || kind->asString() == "scene" || kind->asString() == "route" || kind->asString() == "audioRoute" || kind->asString() == "showInput" || kind->asString() == "output" || kind->asString() == "recorder"))) return false;
  const auto* id = value.get("id");
  if (!id || !(id->isString() && id->asString().size() >= 1)) return false;
  return true;
}
inline rpc::Json toJson(const EntityIdentity& value) {
  rpc::Json::Object result;
  result.emplace("kind", value.kind);
  result.emplace("id", value.id);
  return result;
}
struct EntityRevision {
  std::string kind{};
  std::string id{};
  std::string authorityEpoch{};
  std::int64_t revision{};
};
inline bool validateEntityRevision(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* kind = value.get("kind");
  if (!kind || !(kind->isString() && (kind->asString() == "person" || kind->asString() == "participant" || kind->asString() == "source" || kind->asString() == "scene" || kind->asString() == "route" || kind->asString() == "audioRoute" || kind->asString() == "showInput" || kind->asString() == "output" || kind->asString() == "recorder"))) return false;
  const auto* id = value.get("id");
  if (!id || !(id->isString() && id->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* revision = value.get("revision");
  if (!revision || !(revision->isNumber() && std::floor(revision->asNumber()) == revision->asNumber() && revision->asNumber() >= 0 && revision->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const EntityRevision& value) {
  rpc::Json::Object result;
  result.emplace("kind", value.kind);
  result.emplace("id", value.id);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("revision", static_cast<double>(value.revision));
  return result;
}
struct SourceInstanceIdentity {
  std::string sourceId{};
  std::string instanceId{};
  std::string processEpoch{};
  std::int64_t generation{};
};
inline bool validateSourceInstanceIdentity(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* sourceId = value.get("sourceId");
  if (!sourceId || !(sourceId->isString() && sourceId->asString().size() >= 1)) return false;
  const auto* instanceId = value.get("instanceId");
  if (!instanceId || !(instanceId->isString() && instanceId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* generation = value.get("generation");
  if (!generation || !(generation->isNumber() && std::floor(generation->asNumber()) == generation->asNumber() && generation->asNumber() >= 1 && generation->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const SourceInstanceIdentity& value) {
  rpc::Json::Object result;
  result.emplace("sourceId", value.sourceId);
  result.emplace("instanceId", value.instanceId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("generation", static_cast<double>(value.generation));
  return result;
}
struct ParticipantBindingIdentity {
  std::string participantId{};
  std::string sourceId{};
  std::string instanceId{};
  std::string processEpoch{};
  std::int64_t generation{};
};
inline bool validateParticipantBindingIdentity(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* participantId = value.get("participantId");
  if (!participantId || !(participantId->isString() && participantId->asString().size() >= 1)) return false;
  const auto* sourceId = value.get("sourceId");
  if (!sourceId || !(sourceId->isString() && sourceId->asString().size() >= 1)) return false;
  const auto* instanceId = value.get("instanceId");
  if (!instanceId || !(instanceId->isString() && instanceId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* generation = value.get("generation");
  if (!generation || !(generation->isNumber() && std::floor(generation->asNumber()) == generation->asNumber() && generation->asNumber() >= 1 && generation->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const ParticipantBindingIdentity& value) {
  rpc::Json::Object result;
  result.emplace("participantId", value.participantId);
  result.emplace("sourceId", value.sourceId);
  result.emplace("instanceId", value.instanceId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("generation", static_cast<double>(value.generation));
  return result;
}
struct ControlRevision {
  std::string authorityEpoch{};
  std::int64_t revision{};
};
inline bool validateControlRevision(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* revision = value.get("revision");
  if (!revision || !(revision->isNumber() && std::floor(revision->asNumber()) == revision->asNumber() && revision->asNumber() >= 0 && revision->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const ControlRevision& value) {
  rpc::Json::Object result;
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("revision", static_cast<double>(value.revision));
  return result;
}
struct PlanGeneration {
  std::string kind{};
  std::string planId{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::int64_t generation{};
  std::int64_t clockGeneration{};
};
inline bool validatePlanGeneration(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* kind = value.get("kind");
  if (!kind || !(kind->isString() && (kind->asString() == "render" || kind->asString() == "audio" || kind->asString() == "output"))) return false;
  const auto* planId = value.get("planId");
  if (!planId || !(planId->isString() && planId->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* generation = value.get("generation");
  if (!generation || !(generation->isNumber() && std::floor(generation->asNumber()) == generation->asNumber() && generation->asNumber() >= 1 && generation->asNumber() <= 9007199254740991)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const PlanGeneration& value) {
  rpc::Json::Object result;
  result.emplace("kind", value.kind);
  result.emplace("planId", value.planId);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("generation", static_cast<double>(value.generation));
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  return result;
}
struct ControlOperationIdentity {
  std::string operationId{};
  std::string authorityEpoch{};
  std::int64_t expectedRevision{};
};
inline bool validateControlOperationIdentity(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* operationId = value.get("operationId");
  if (!operationId || !(operationId->isString() && operationId->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* expectedRevision = value.get("expectedRevision");
  if (!expectedRevision || !(expectedRevision->isNumber() && std::floor(expectedRevision->asNumber()) == expectedRevision->asNumber() && expectedRevision->asNumber() >= 0 && expectedRevision->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const ControlOperationIdentity& value) {
  rpc::Json::Object result;
  result.emplace("operationId", value.operationId);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("expectedRevision", static_cast<double>(value.expectedRevision));
  return result;
}
struct AcceptedOperationObservation {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string stage{};
  std::string operationId{};
  std::int64_t expectedRevision{};
};
inline bool validateAcceptedOperationObservation(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* stage = value.get("stage");
  if (!stage || !(stage->isString() && (stage->asString() == "accepted"))) return false;
  const auto* operationId = value.get("operationId");
  if (!operationId || !(operationId->isString() && operationId->asString().size() >= 1)) return false;
  const auto* expectedRevision = value.get("expectedRevision");
  if (!expectedRevision || !(expectedRevision->isNumber() && std::floor(expectedRevision->asNumber()) == expectedRevision->asNumber() && expectedRevision->asNumber() >= 0 && expectedRevision->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const AcceptedOperationObservation& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("stage", value.stage);
  result.emplace("operationId", value.operationId);
  result.emplace("expectedRevision", static_cast<double>(value.expectedRevision));
  return result;
}
struct AppliedOperationObservation {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string stage{};
  std::string operationId{};
  std::int64_t appliedRevision{};
};
inline bool validateAppliedOperationObservation(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* stage = value.get("stage");
  if (!stage || !(stage->isString() && (stage->asString() == "applied"))) return false;
  const auto* operationId = value.get("operationId");
  if (!operationId || !(operationId->isString() && operationId->asString().size() >= 1)) return false;
  const auto* appliedRevision = value.get("appliedRevision");
  if (!appliedRevision || !(appliedRevision->isNumber() && std::floor(appliedRevision->asNumber()) == appliedRevision->asNumber() && appliedRevision->asNumber() >= 0 && appliedRevision->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const AppliedOperationObservation& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("stage", value.stage);
  result.emplace("operationId", value.operationId);
  result.emplace("appliedRevision", static_cast<double>(value.appliedRevision));
  return result;
}
struct RenderedMediaObservation {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string stage{};
  std::string planId{};
  std::int64_t planGeneration{};
  std::string streamId{};
  std::int64_t streamGeneration{};
  std::string mediaKind{};
  std::int64_t sequenceStart{};
  std::int64_t sequenceCount{};
  std::int64_t contentStartNs{};
  std::int64_t contentDurationNs{};
  std::string resourceId{};
  std::string leaseId{};
  std::int64_t leaseGeneration{};
  std::string completionToken{};
};
inline bool validateRenderedMediaObservation(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* stage = value.get("stage");
  if (!stage || !(stage->isString() && (stage->asString() == "rendered"))) return false;
  const auto* planId = value.get("planId");
  if (!planId || !(planId->isString() && planId->asString().size() >= 1)) return false;
  const auto* planGeneration = value.get("planGeneration");
  if (!planGeneration || !(planGeneration->isNumber() && std::floor(planGeneration->asNumber()) == planGeneration->asNumber() && planGeneration->asNumber() >= 1 && planGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* streamId = value.get("streamId");
  if (!streamId || !(streamId->isString() && streamId->asString().size() >= 1)) return false;
  const auto* streamGeneration = value.get("streamGeneration");
  if (!streamGeneration || !(streamGeneration->isNumber() && std::floor(streamGeneration->asNumber()) == streamGeneration->asNumber() && streamGeneration->asNumber() >= 1 && streamGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* mediaKind = value.get("mediaKind");
  if (!mediaKind || !(mediaKind->isString() && (mediaKind->asString() == "audio" || mediaKind->asString() == "video"))) return false;
  const auto* sequenceStart = value.get("sequenceStart");
  if (!sequenceStart || !(sequenceStart->isNumber() && std::floor(sequenceStart->asNumber()) == sequenceStart->asNumber() && sequenceStart->asNumber() >= 0 && sequenceStart->asNumber() <= 9007199254740991)) return false;
  const auto* sequenceCount = value.get("sequenceCount");
  if (!sequenceCount || !(sequenceCount->isNumber() && std::floor(sequenceCount->asNumber()) == sequenceCount->asNumber() && sequenceCount->asNumber() >= 1 && sequenceCount->asNumber() <= 9007199254740991)) return false;
  const auto* contentStartNs = value.get("contentStartNs");
  if (!contentStartNs || !(contentStartNs->isNumber() && std::floor(contentStartNs->asNumber()) == contentStartNs->asNumber() && contentStartNs->asNumber() >= 0 && contentStartNs->asNumber() <= 9007199254740991)) return false;
  const auto* contentDurationNs = value.get("contentDurationNs");
  if (!contentDurationNs || !(contentDurationNs->isNumber() && std::floor(contentDurationNs->asNumber()) == contentDurationNs->asNumber() && contentDurationNs->asNumber() >= 1 && contentDurationNs->asNumber() <= 9007199254740991)) return false;
  const auto* resourceId = value.get("resourceId");
  if (!resourceId || !(resourceId->isString() && resourceId->asString().size() >= 1)) return false;
  const auto* leaseId = value.get("leaseId");
  if (!leaseId || !(leaseId->isString() && leaseId->asString().size() >= 1)) return false;
  const auto* leaseGeneration = value.get("leaseGeneration");
  if (!leaseGeneration || !(leaseGeneration->isNumber() && std::floor(leaseGeneration->asNumber()) == leaseGeneration->asNumber() && leaseGeneration->asNumber() >= 1 && leaseGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* completionToken = value.get("completionToken");
  if (!completionToken || !(completionToken->isString() && completionToken->asString().size() >= 1)) return false;
  return true;
}
inline rpc::Json toJson(const RenderedMediaObservation& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("stage", value.stage);
  result.emplace("planId", value.planId);
  result.emplace("planGeneration", static_cast<double>(value.planGeneration));
  result.emplace("streamId", value.streamId);
  result.emplace("streamGeneration", static_cast<double>(value.streamGeneration));
  result.emplace("mediaKind", value.mediaKind);
  result.emplace("sequenceStart", static_cast<double>(value.sequenceStart));
  result.emplace("sequenceCount", static_cast<double>(value.sequenceCount));
  result.emplace("contentStartNs", static_cast<double>(value.contentStartNs));
  result.emplace("contentDurationNs", static_cast<double>(value.contentDurationNs));
  result.emplace("resourceId", value.resourceId);
  result.emplace("leaseId", value.leaseId);
  result.emplace("leaseGeneration", static_cast<double>(value.leaseGeneration));
  result.emplace("completionToken", value.completionToken);
  return result;
}
struct DeliveredMediaObservation {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string stage{};
  std::string planId{};
  std::int64_t planGeneration{};
  std::string streamId{};
  std::int64_t streamGeneration{};
  std::string mediaKind{};
  std::int64_t sequenceStart{};
  std::int64_t sequenceCount{};
  std::int64_t contentStartNs{};
  std::int64_t contentDurationNs{};
  std::string destinationId{};
  std::int64_t destinationGeneration{};
  std::string sessionId{};
  std::string deliveryId{};
  std::int64_t scheduledAtNs{};
  std::int64_t consumedAtNs{};
};
inline bool validateDeliveredMediaObservation(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* stage = value.get("stage");
  if (!stage || !(stage->isString() && (stage->asString() == "delivered"))) return false;
  const auto* planId = value.get("planId");
  if (!planId || !(planId->isString() && planId->asString().size() >= 1)) return false;
  const auto* planGeneration = value.get("planGeneration");
  if (!planGeneration || !(planGeneration->isNumber() && std::floor(planGeneration->asNumber()) == planGeneration->asNumber() && planGeneration->asNumber() >= 1 && planGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* streamId = value.get("streamId");
  if (!streamId || !(streamId->isString() && streamId->asString().size() >= 1)) return false;
  const auto* streamGeneration = value.get("streamGeneration");
  if (!streamGeneration || !(streamGeneration->isNumber() && std::floor(streamGeneration->asNumber()) == streamGeneration->asNumber() && streamGeneration->asNumber() >= 1 && streamGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* mediaKind = value.get("mediaKind");
  if (!mediaKind || !(mediaKind->isString() && (mediaKind->asString() == "audio" || mediaKind->asString() == "video"))) return false;
  const auto* sequenceStart = value.get("sequenceStart");
  if (!sequenceStart || !(sequenceStart->isNumber() && std::floor(sequenceStart->asNumber()) == sequenceStart->asNumber() && sequenceStart->asNumber() >= 0 && sequenceStart->asNumber() <= 9007199254740991)) return false;
  const auto* sequenceCount = value.get("sequenceCount");
  if (!sequenceCount || !(sequenceCount->isNumber() && std::floor(sequenceCount->asNumber()) == sequenceCount->asNumber() && sequenceCount->asNumber() >= 1 && sequenceCount->asNumber() <= 9007199254740991)) return false;
  const auto* contentStartNs = value.get("contentStartNs");
  if (!contentStartNs || !(contentStartNs->isNumber() && std::floor(contentStartNs->asNumber()) == contentStartNs->asNumber() && contentStartNs->asNumber() >= 0 && contentStartNs->asNumber() <= 9007199254740991)) return false;
  const auto* contentDurationNs = value.get("contentDurationNs");
  if (!contentDurationNs || !(contentDurationNs->isNumber() && std::floor(contentDurationNs->asNumber()) == contentDurationNs->asNumber() && contentDurationNs->asNumber() >= 1 && contentDurationNs->asNumber() <= 9007199254740991)) return false;
  const auto* destinationId = value.get("destinationId");
  if (!destinationId || !(destinationId->isString() && destinationId->asString().size() >= 1)) return false;
  const auto* destinationGeneration = value.get("destinationGeneration");
  if (!destinationGeneration || !(destinationGeneration->isNumber() && std::floor(destinationGeneration->asNumber()) == destinationGeneration->asNumber() && destinationGeneration->asNumber() >= 1 && destinationGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* sessionId = value.get("sessionId");
  if (!sessionId || !(sessionId->isString() && sessionId->asString().size() >= 1)) return false;
  const auto* deliveryId = value.get("deliveryId");
  if (!deliveryId || !(deliveryId->isString() && deliveryId->asString().size() >= 1)) return false;
  const auto* scheduledAtNs = value.get("scheduledAtNs");
  if (!scheduledAtNs || !(scheduledAtNs->isNumber() && std::floor(scheduledAtNs->asNumber()) == scheduledAtNs->asNumber() && scheduledAtNs->asNumber() >= 0 && scheduledAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* consumedAtNs = value.get("consumedAtNs");
  if (!consumedAtNs || !(consumedAtNs->isNumber() && std::floor(consumedAtNs->asNumber()) == consumedAtNs->asNumber() && consumedAtNs->asNumber() >= 0 && consumedAtNs->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const DeliveredMediaObservation& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("stage", value.stage);
  result.emplace("planId", value.planId);
  result.emplace("planGeneration", static_cast<double>(value.planGeneration));
  result.emplace("streamId", value.streamId);
  result.emplace("streamGeneration", static_cast<double>(value.streamGeneration));
  result.emplace("mediaKind", value.mediaKind);
  result.emplace("sequenceStart", static_cast<double>(value.sequenceStart));
  result.emplace("sequenceCount", static_cast<double>(value.sequenceCount));
  result.emplace("contentStartNs", static_cast<double>(value.contentStartNs));
  result.emplace("contentDurationNs", static_cast<double>(value.contentDurationNs));
  result.emplace("destinationId", value.destinationId);
  result.emplace("destinationGeneration", static_cast<double>(value.destinationGeneration));
  result.emplace("sessionId", value.sessionId);
  result.emplace("deliveryId", value.deliveryId);
  result.emplace("scheduledAtNs", static_cast<double>(value.scheduledAtNs));
  result.emplace("consumedAtNs", static_cast<double>(value.consumedAtNs));
  return result;
}
struct PresentedMediaObservation {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string stage{};
  std::string planId{};
  std::int64_t planGeneration{};
  std::string streamId{};
  std::int64_t streamGeneration{};
  std::string mediaKind{};
  std::int64_t sequenceStart{};
  std::int64_t sequenceCount{};
  std::int64_t contentStartNs{};
  std::int64_t contentDurationNs{};
  std::string destinationId{};
  std::int64_t destinationGeneration{};
  std::string sessionId{};
  std::string presentationId{};
  std::int64_t presentedAtNs{};
};
inline bool validatePresentedMediaObservation(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* stage = value.get("stage");
  if (!stage || !(stage->isString() && (stage->asString() == "presented"))) return false;
  const auto* planId = value.get("planId");
  if (!planId || !(planId->isString() && planId->asString().size() >= 1)) return false;
  const auto* planGeneration = value.get("planGeneration");
  if (!planGeneration || !(planGeneration->isNumber() && std::floor(planGeneration->asNumber()) == planGeneration->asNumber() && planGeneration->asNumber() >= 1 && planGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* streamId = value.get("streamId");
  if (!streamId || !(streamId->isString() && streamId->asString().size() >= 1)) return false;
  const auto* streamGeneration = value.get("streamGeneration");
  if (!streamGeneration || !(streamGeneration->isNumber() && std::floor(streamGeneration->asNumber()) == streamGeneration->asNumber() && streamGeneration->asNumber() >= 1 && streamGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* mediaKind = value.get("mediaKind");
  if (!mediaKind || !(mediaKind->isString() && (mediaKind->asString() == "audio" || mediaKind->asString() == "video"))) return false;
  const auto* sequenceStart = value.get("sequenceStart");
  if (!sequenceStart || !(sequenceStart->isNumber() && std::floor(sequenceStart->asNumber()) == sequenceStart->asNumber() && sequenceStart->asNumber() >= 0 && sequenceStart->asNumber() <= 9007199254740991)) return false;
  const auto* sequenceCount = value.get("sequenceCount");
  if (!sequenceCount || !(sequenceCount->isNumber() && std::floor(sequenceCount->asNumber()) == sequenceCount->asNumber() && sequenceCount->asNumber() >= 1 && sequenceCount->asNumber() <= 9007199254740991)) return false;
  const auto* contentStartNs = value.get("contentStartNs");
  if (!contentStartNs || !(contentStartNs->isNumber() && std::floor(contentStartNs->asNumber()) == contentStartNs->asNumber() && contentStartNs->asNumber() >= 0 && contentStartNs->asNumber() <= 9007199254740991)) return false;
  const auto* contentDurationNs = value.get("contentDurationNs");
  if (!contentDurationNs || !(contentDurationNs->isNumber() && std::floor(contentDurationNs->asNumber()) == contentDurationNs->asNumber() && contentDurationNs->asNumber() >= 1 && contentDurationNs->asNumber() <= 9007199254740991)) return false;
  const auto* destinationId = value.get("destinationId");
  if (!destinationId || !(destinationId->isString() && destinationId->asString().size() >= 1)) return false;
  const auto* destinationGeneration = value.get("destinationGeneration");
  if (!destinationGeneration || !(destinationGeneration->isNumber() && std::floor(destinationGeneration->asNumber()) == destinationGeneration->asNumber() && destinationGeneration->asNumber() >= 1 && destinationGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* sessionId = value.get("sessionId");
  if (!sessionId || !(sessionId->isString() && sessionId->asString().size() >= 1)) return false;
  const auto* presentationId = value.get("presentationId");
  if (!presentationId || !(presentationId->isString() && presentationId->asString().size() >= 1)) return false;
  const auto* presentedAtNs = value.get("presentedAtNs");
  if (!presentedAtNs || !(presentedAtNs->isNumber() && std::floor(presentedAtNs->asNumber()) == presentedAtNs->asNumber() && presentedAtNs->asNumber() >= 0 && presentedAtNs->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const PresentedMediaObservation& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("stage", value.stage);
  result.emplace("planId", value.planId);
  result.emplace("planGeneration", static_cast<double>(value.planGeneration));
  result.emplace("streamId", value.streamId);
  result.emplace("streamGeneration", static_cast<double>(value.streamGeneration));
  result.emplace("mediaKind", value.mediaKind);
  result.emplace("sequenceStart", static_cast<double>(value.sequenceStart));
  result.emplace("sequenceCount", static_cast<double>(value.sequenceCount));
  result.emplace("contentStartNs", static_cast<double>(value.contentStartNs));
  result.emplace("contentDurationNs", static_cast<double>(value.contentDurationNs));
  result.emplace("destinationId", value.destinationId);
  result.emplace("destinationGeneration", static_cast<double>(value.destinationGeneration));
  result.emplace("sessionId", value.sessionId);
  result.emplace("presentationId", value.presentationId);
  result.emplace("presentedAtNs", static_cast<double>(value.presentedAtNs));
  return result;
}
struct MuxedMediaObservation {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string stage{};
  std::string planId{};
  std::int64_t planGeneration{};
  std::string streamId{};
  std::int64_t streamGeneration{};
  std::string mediaKind{};
  std::int64_t sequenceStart{};
  std::int64_t sequenceCount{};
  std::int64_t contentStartNs{};
  std::int64_t contentDurationNs{};
  std::string destinationId{};
  std::int64_t destinationGeneration{};
  std::string sessionId{};
  std::string artifactId{};
  std::int64_t packetStart{};
  std::int64_t packetCount{};
};
inline bool validateMuxedMediaObservation(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* stage = value.get("stage");
  if (!stage || !(stage->isString() && (stage->asString() == "muxed"))) return false;
  const auto* planId = value.get("planId");
  if (!planId || !(planId->isString() && planId->asString().size() >= 1)) return false;
  const auto* planGeneration = value.get("planGeneration");
  if (!planGeneration || !(planGeneration->isNumber() && std::floor(planGeneration->asNumber()) == planGeneration->asNumber() && planGeneration->asNumber() >= 1 && planGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* streamId = value.get("streamId");
  if (!streamId || !(streamId->isString() && streamId->asString().size() >= 1)) return false;
  const auto* streamGeneration = value.get("streamGeneration");
  if (!streamGeneration || !(streamGeneration->isNumber() && std::floor(streamGeneration->asNumber()) == streamGeneration->asNumber() && streamGeneration->asNumber() >= 1 && streamGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* mediaKind = value.get("mediaKind");
  if (!mediaKind || !(mediaKind->isString() && (mediaKind->asString() == "audio" || mediaKind->asString() == "video"))) return false;
  const auto* sequenceStart = value.get("sequenceStart");
  if (!sequenceStart || !(sequenceStart->isNumber() && std::floor(sequenceStart->asNumber()) == sequenceStart->asNumber() && sequenceStart->asNumber() >= 0 && sequenceStart->asNumber() <= 9007199254740991)) return false;
  const auto* sequenceCount = value.get("sequenceCount");
  if (!sequenceCount || !(sequenceCount->isNumber() && std::floor(sequenceCount->asNumber()) == sequenceCount->asNumber() && sequenceCount->asNumber() >= 1 && sequenceCount->asNumber() <= 9007199254740991)) return false;
  const auto* contentStartNs = value.get("contentStartNs");
  if (!contentStartNs || !(contentStartNs->isNumber() && std::floor(contentStartNs->asNumber()) == contentStartNs->asNumber() && contentStartNs->asNumber() >= 0 && contentStartNs->asNumber() <= 9007199254740991)) return false;
  const auto* contentDurationNs = value.get("contentDurationNs");
  if (!contentDurationNs || !(contentDurationNs->isNumber() && std::floor(contentDurationNs->asNumber()) == contentDurationNs->asNumber() && contentDurationNs->asNumber() >= 1 && contentDurationNs->asNumber() <= 9007199254740991)) return false;
  const auto* destinationId = value.get("destinationId");
  if (!destinationId || !(destinationId->isString() && destinationId->asString().size() >= 1)) return false;
  const auto* destinationGeneration = value.get("destinationGeneration");
  if (!destinationGeneration || !(destinationGeneration->isNumber() && std::floor(destinationGeneration->asNumber()) == destinationGeneration->asNumber() && destinationGeneration->asNumber() >= 1 && destinationGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* sessionId = value.get("sessionId");
  if (!sessionId || !(sessionId->isString() && sessionId->asString().size() >= 1)) return false;
  const auto* artifactId = value.get("artifactId");
  if (!artifactId || !(artifactId->isString() && artifactId->asString().size() >= 1)) return false;
  const auto* packetStart = value.get("packetStart");
  if (!packetStart || !(packetStart->isNumber() && std::floor(packetStart->asNumber()) == packetStart->asNumber() && packetStart->asNumber() >= 0 && packetStart->asNumber() <= 9007199254740991)) return false;
  const auto* packetCount = value.get("packetCount");
  if (!packetCount || !(packetCount->isNumber() && std::floor(packetCount->asNumber()) == packetCount->asNumber() && packetCount->asNumber() >= 1 && packetCount->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const MuxedMediaObservation& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("stage", value.stage);
  result.emplace("planId", value.planId);
  result.emplace("planGeneration", static_cast<double>(value.planGeneration));
  result.emplace("streamId", value.streamId);
  result.emplace("streamGeneration", static_cast<double>(value.streamGeneration));
  result.emplace("mediaKind", value.mediaKind);
  result.emplace("sequenceStart", static_cast<double>(value.sequenceStart));
  result.emplace("sequenceCount", static_cast<double>(value.sequenceCount));
  result.emplace("contentStartNs", static_cast<double>(value.contentStartNs));
  result.emplace("contentDurationNs", static_cast<double>(value.contentDurationNs));
  result.emplace("destinationId", value.destinationId);
  result.emplace("destinationGeneration", static_cast<double>(value.destinationGeneration));
  result.emplace("sessionId", value.sessionId);
  result.emplace("artifactId", value.artifactId);
  result.emplace("packetStart", static_cast<double>(value.packetStart));
  result.emplace("packetCount", static_cast<double>(value.packetCount));
  return result;
}
struct CommittedMediaObservation {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string stage{};
  std::string planId{};
  std::int64_t planGeneration{};
  std::string streamId{};
  std::int64_t streamGeneration{};
  std::string mediaKind{};
  std::int64_t sequenceStart{};
  std::int64_t sequenceCount{};
  std::int64_t contentStartNs{};
  std::int64_t contentDurationNs{};
  std::string destinationId{};
  std::int64_t destinationGeneration{};
  std::string sessionId{};
  std::string artifactId{};
  std::string commitId{};
  std::int64_t byteOffset{};
  std::int64_t byteCount{};
};
inline bool validateCommittedMediaObservation(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* stage = value.get("stage");
  if (!stage || !(stage->isString() && (stage->asString() == "committed"))) return false;
  const auto* planId = value.get("planId");
  if (!planId || !(planId->isString() && planId->asString().size() >= 1)) return false;
  const auto* planGeneration = value.get("planGeneration");
  if (!planGeneration || !(planGeneration->isNumber() && std::floor(planGeneration->asNumber()) == planGeneration->asNumber() && planGeneration->asNumber() >= 1 && planGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* streamId = value.get("streamId");
  if (!streamId || !(streamId->isString() && streamId->asString().size() >= 1)) return false;
  const auto* streamGeneration = value.get("streamGeneration");
  if (!streamGeneration || !(streamGeneration->isNumber() && std::floor(streamGeneration->asNumber()) == streamGeneration->asNumber() && streamGeneration->asNumber() >= 1 && streamGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* mediaKind = value.get("mediaKind");
  if (!mediaKind || !(mediaKind->isString() && (mediaKind->asString() == "audio" || mediaKind->asString() == "video"))) return false;
  const auto* sequenceStart = value.get("sequenceStart");
  if (!sequenceStart || !(sequenceStart->isNumber() && std::floor(sequenceStart->asNumber()) == sequenceStart->asNumber() && sequenceStart->asNumber() >= 0 && sequenceStart->asNumber() <= 9007199254740991)) return false;
  const auto* sequenceCount = value.get("sequenceCount");
  if (!sequenceCount || !(sequenceCount->isNumber() && std::floor(sequenceCount->asNumber()) == sequenceCount->asNumber() && sequenceCount->asNumber() >= 1 && sequenceCount->asNumber() <= 9007199254740991)) return false;
  const auto* contentStartNs = value.get("contentStartNs");
  if (!contentStartNs || !(contentStartNs->isNumber() && std::floor(contentStartNs->asNumber()) == contentStartNs->asNumber() && contentStartNs->asNumber() >= 0 && contentStartNs->asNumber() <= 9007199254740991)) return false;
  const auto* contentDurationNs = value.get("contentDurationNs");
  if (!contentDurationNs || !(contentDurationNs->isNumber() && std::floor(contentDurationNs->asNumber()) == contentDurationNs->asNumber() && contentDurationNs->asNumber() >= 1 && contentDurationNs->asNumber() <= 9007199254740991)) return false;
  const auto* destinationId = value.get("destinationId");
  if (!destinationId || !(destinationId->isString() && destinationId->asString().size() >= 1)) return false;
  const auto* destinationGeneration = value.get("destinationGeneration");
  if (!destinationGeneration || !(destinationGeneration->isNumber() && std::floor(destinationGeneration->asNumber()) == destinationGeneration->asNumber() && destinationGeneration->asNumber() >= 1 && destinationGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* sessionId = value.get("sessionId");
  if (!sessionId || !(sessionId->isString() && sessionId->asString().size() >= 1)) return false;
  const auto* artifactId = value.get("artifactId");
  if (!artifactId || !(artifactId->isString() && artifactId->asString().size() >= 1)) return false;
  const auto* commitId = value.get("commitId");
  if (!commitId || !(commitId->isString() && commitId->asString().size() >= 1)) return false;
  const auto* byteOffset = value.get("byteOffset");
  if (!byteOffset || !(byteOffset->isNumber() && std::floor(byteOffset->asNumber()) == byteOffset->asNumber() && byteOffset->asNumber() >= 0 && byteOffset->asNumber() <= 9007199254740991)) return false;
  const auto* byteCount = value.get("byteCount");
  if (!byteCount || !(byteCount->isNumber() && std::floor(byteCount->asNumber()) == byteCount->asNumber() && byteCount->asNumber() >= 1 && byteCount->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const CommittedMediaObservation& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("stage", value.stage);
  result.emplace("planId", value.planId);
  result.emplace("planGeneration", static_cast<double>(value.planGeneration));
  result.emplace("streamId", value.streamId);
  result.emplace("streamGeneration", static_cast<double>(value.streamGeneration));
  result.emplace("mediaKind", value.mediaKind);
  result.emplace("sequenceStart", static_cast<double>(value.sequenceStart));
  result.emplace("sequenceCount", static_cast<double>(value.sequenceCount));
  result.emplace("contentStartNs", static_cast<double>(value.contentStartNs));
  result.emplace("contentDurationNs", static_cast<double>(value.contentDurationNs));
  result.emplace("destinationId", value.destinationId);
  result.emplace("destinationGeneration", static_cast<double>(value.destinationGeneration));
  result.emplace("sessionId", value.sessionId);
  result.emplace("artifactId", value.artifactId);
  result.emplace("commitId", value.commitId);
  result.emplace("byteOffset", static_cast<double>(value.byteOffset));
  result.emplace("byteCount", static_cast<double>(value.byteCount));
  return result;
}
struct CompletedOutputObservation {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string stage{};
  std::string destinationId{};
  std::int64_t destinationGeneration{};
  std::string sessionId{};
  std::string completionId{};
  std::string finalPlanId{};
  std::int64_t finalPlanGeneration{};
  std::string finalStreamId{};
  std::int64_t finalStreamGeneration{};
  std::string mediaKind{};
  std::int64_t deliveredUnits{};
  std::int64_t muxedPackets{};
  std::int64_t committedBytes{};
};
inline bool validateCompletedOutputObservation(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* stage = value.get("stage");
  if (!stage || !(stage->isString() && (stage->asString() == "completed"))) return false;
  const auto* destinationId = value.get("destinationId");
  if (!destinationId || !(destinationId->isString() && destinationId->asString().size() >= 1)) return false;
  const auto* destinationGeneration = value.get("destinationGeneration");
  if (!destinationGeneration || !(destinationGeneration->isNumber() && std::floor(destinationGeneration->asNumber()) == destinationGeneration->asNumber() && destinationGeneration->asNumber() >= 1 && destinationGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* sessionId = value.get("sessionId");
  if (!sessionId || !(sessionId->isString() && sessionId->asString().size() >= 1)) return false;
  const auto* completionId = value.get("completionId");
  if (!completionId || !(completionId->isString() && completionId->asString().size() >= 1)) return false;
  const auto* finalPlanId = value.get("finalPlanId");
  if (!finalPlanId || !(finalPlanId->isString() && finalPlanId->asString().size() >= 1)) return false;
  const auto* finalPlanGeneration = value.get("finalPlanGeneration");
  if (!finalPlanGeneration || !(finalPlanGeneration->isNumber() && std::floor(finalPlanGeneration->asNumber()) == finalPlanGeneration->asNumber() && finalPlanGeneration->asNumber() >= 1 && finalPlanGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* finalStreamId = value.get("finalStreamId");
  if (!finalStreamId || !(finalStreamId->isString() && finalStreamId->asString().size() >= 1)) return false;
  const auto* finalStreamGeneration = value.get("finalStreamGeneration");
  if (!finalStreamGeneration || !(finalStreamGeneration->isNumber() && std::floor(finalStreamGeneration->asNumber()) == finalStreamGeneration->asNumber() && finalStreamGeneration->asNumber() >= 1 && finalStreamGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* mediaKind = value.get("mediaKind");
  if (!mediaKind || !(mediaKind->isString() && (mediaKind->asString() == "audio" || mediaKind->asString() == "video"))) return false;
  const auto* deliveredUnits = value.get("deliveredUnits");
  if (!deliveredUnits || !(deliveredUnits->isNumber() && std::floor(deliveredUnits->asNumber()) == deliveredUnits->asNumber() && deliveredUnits->asNumber() >= 0 && deliveredUnits->asNumber() <= 9007199254740991)) return false;
  const auto* muxedPackets = value.get("muxedPackets");
  if (!muxedPackets || !(muxedPackets->isNumber() && std::floor(muxedPackets->asNumber()) == muxedPackets->asNumber() && muxedPackets->asNumber() >= 0 && muxedPackets->asNumber() <= 9007199254740991)) return false;
  const auto* committedBytes = value.get("committedBytes");
  if (!committedBytes || !(committedBytes->isNumber() && std::floor(committedBytes->asNumber()) == committedBytes->asNumber() && committedBytes->asNumber() >= 0 && committedBytes->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const CompletedOutputObservation& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("stage", value.stage);
  result.emplace("destinationId", value.destinationId);
  result.emplace("destinationGeneration", static_cast<double>(value.destinationGeneration));
  result.emplace("sessionId", value.sessionId);
  result.emplace("completionId", value.completionId);
  result.emplace("finalPlanId", value.finalPlanId);
  result.emplace("finalPlanGeneration", static_cast<double>(value.finalPlanGeneration));
  result.emplace("finalStreamId", value.finalStreamId);
  result.emplace("finalStreamGeneration", static_cast<double>(value.finalStreamGeneration));
  result.emplace("mediaKind", value.mediaKind);
  result.emplace("deliveredUnits", static_cast<double>(value.deliveredUnits));
  result.emplace("muxedPackets", static_cast<double>(value.muxedPackets));
  result.emplace("committedBytes", static_cast<double>(value.committedBytes));
  return result;
}
struct ResourceLeaseDescriptor {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string resourceId{};
  std::string leaseId{};
  std::int64_t leaseGeneration{};
  std::string resourceKind{};
  std::string ownerDomain{};
  std::string state{};
  std::int64_t byteLength{};
};
inline bool validateResourceLeaseDescriptor(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* resourceId = value.get("resourceId");
  if (!resourceId || !(resourceId->isString() && resourceId->asString().size() >= 1)) return false;
  const auto* leaseId = value.get("leaseId");
  if (!leaseId || !(leaseId->isString() && leaseId->asString().size() >= 1)) return false;
  const auto* leaseGeneration = value.get("leaseGeneration");
  if (!leaseGeneration || !(leaseGeneration->isNumber() && std::floor(leaseGeneration->asNumber()) == leaseGeneration->asNumber() && leaseGeneration->asNumber() >= 1 && leaseGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* resourceKind = value.get("resourceKind");
  if (!resourceKind || !(resourceKind->isString() && (resourceKind->asString() == "cpuBuffer" || resourceKind->asString() == "gpuTexture" || resourceKind->asString() == "sharedMemory" || resourceKind->asString() == "encoder" || resourceKind->asString() == "sdkSubscription"))) return false;
  const auto* ownerDomain = value.get("ownerDomain");
  if (!ownerDomain || !(ownerDomain->isString() && ownerDomain->asString().size() >= 1)) return false;
  const auto* state = value.get("state");
  if (!state || !(state->isString() && (state->asString() == "active" || state->asString() == "retiring" || state->asString() == "released"))) return false;
  const auto* byteLength = value.get("byteLength");
  if (!byteLength || !(byteLength->isNumber() && std::floor(byteLength->asNumber()) == byteLength->asNumber() && byteLength->asNumber() >= 0 && byteLength->asNumber() <= 9007199254740991)) return false;
  return true;
}
inline rpc::Json toJson(const ResourceLeaseDescriptor& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("resourceId", value.resourceId);
  result.emplace("leaseId", value.leaseId);
  result.emplace("leaseGeneration", static_cast<double>(value.leaseGeneration));
  result.emplace("resourceKind", value.resourceKind);
  result.emplace("ownerDomain", value.ownerDomain);
  result.emplace("state", value.state);
  result.emplace("byteLength", static_cast<double>(value.byteLength));
  return result;
}
struct DestinationProgress {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string destinationId{};
  std::int64_t destinationGeneration{};
  std::string sessionId{};
  std::string planId{};
  std::int64_t planGeneration{};
  std::string streamId{};
  std::int64_t streamGeneration{};
  std::string mediaKind{};
  std::string state{};
  std::int64_t producedUnits{};
  std::int64_t deliveredUnits{};
  std::int64_t presentedUnits{};
  std::int64_t muxedPackets{};
  std::int64_t committedBytes{};
  std::int64_t deadlineMisses{};
  std::int64_t droppedUnits{};
  std::string counterEpoch{};
};
inline bool validateDestinationProgress(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* destinationId = value.get("destinationId");
  if (!destinationId || !(destinationId->isString() && destinationId->asString().size() >= 1)) return false;
  const auto* destinationGeneration = value.get("destinationGeneration");
  if (!destinationGeneration || !(destinationGeneration->isNumber() && std::floor(destinationGeneration->asNumber()) == destinationGeneration->asNumber() && destinationGeneration->asNumber() >= 1 && destinationGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* sessionId = value.get("sessionId");
  if (!sessionId || !(sessionId->isString() && sessionId->asString().size() >= 1)) return false;
  const auto* planId = value.get("planId");
  if (!planId || !(planId->isString() && planId->asString().size() >= 1)) return false;
  const auto* planGeneration = value.get("planGeneration");
  if (!planGeneration || !(planGeneration->isNumber() && std::floor(planGeneration->asNumber()) == planGeneration->asNumber() && planGeneration->asNumber() >= 1 && planGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* streamId = value.get("streamId");
  if (!streamId || !(streamId->isString() && streamId->asString().size() >= 1)) return false;
  const auto* streamGeneration = value.get("streamGeneration");
  if (!streamGeneration || !(streamGeneration->isNumber() && std::floor(streamGeneration->asNumber()) == streamGeneration->asNumber() && streamGeneration->asNumber() >= 1 && streamGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* mediaKind = value.get("mediaKind");
  if (!mediaKind || !(mediaKind->isString() && (mediaKind->asString() == "audio" || mediaKind->asString() == "video"))) return false;
  const auto* state = value.get("state");
  if (!state || !(state->isString() && (state->asString() == "idle" || state->asString() == "preparing" || state->asString() == "ready" || state->asString() == "running" || state->asString() == "finalizing" || state->asString() == "completed" || state->asString() == "failed" || state->asString() == "interrupted"))) return false;
  const auto* producedUnits = value.get("producedUnits");
  if (!producedUnits || !(producedUnits->isNumber() && std::floor(producedUnits->asNumber()) == producedUnits->asNumber() && producedUnits->asNumber() >= 0 && producedUnits->asNumber() <= 9007199254740991)) return false;
  const auto* deliveredUnits = value.get("deliveredUnits");
  if (!deliveredUnits || !(deliveredUnits->isNumber() && std::floor(deliveredUnits->asNumber()) == deliveredUnits->asNumber() && deliveredUnits->asNumber() >= 0 && deliveredUnits->asNumber() <= 9007199254740991)) return false;
  const auto* presentedUnits = value.get("presentedUnits");
  if (!presentedUnits || !(presentedUnits->isNumber() && std::floor(presentedUnits->asNumber()) == presentedUnits->asNumber() && presentedUnits->asNumber() >= 0 && presentedUnits->asNumber() <= 9007199254740991)) return false;
  const auto* muxedPackets = value.get("muxedPackets");
  if (!muxedPackets || !(muxedPackets->isNumber() && std::floor(muxedPackets->asNumber()) == muxedPackets->asNumber() && muxedPackets->asNumber() >= 0 && muxedPackets->asNumber() <= 9007199254740991)) return false;
  const auto* committedBytes = value.get("committedBytes");
  if (!committedBytes || !(committedBytes->isNumber() && std::floor(committedBytes->asNumber()) == committedBytes->asNumber() && committedBytes->asNumber() >= 0 && committedBytes->asNumber() <= 9007199254740991)) return false;
  const auto* deadlineMisses = value.get("deadlineMisses");
  if (!deadlineMisses || !(deadlineMisses->isNumber() && std::floor(deadlineMisses->asNumber()) == deadlineMisses->asNumber() && deadlineMisses->asNumber() >= 0 && deadlineMisses->asNumber() <= 9007199254740991)) return false;
  const auto* droppedUnits = value.get("droppedUnits");
  if (!droppedUnits || !(droppedUnits->isNumber() && std::floor(droppedUnits->asNumber()) == droppedUnits->asNumber() && droppedUnits->asNumber() >= 0 && droppedUnits->asNumber() <= 9007199254740991)) return false;
  const auto* counterEpoch = value.get("counterEpoch");
  if (!counterEpoch || !(counterEpoch->isString() && counterEpoch->asString().size() >= 1)) return false;
  return true;
}
inline rpc::Json toJson(const DestinationProgress& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("destinationId", value.destinationId);
  result.emplace("destinationGeneration", static_cast<double>(value.destinationGeneration));
  result.emplace("sessionId", value.sessionId);
  result.emplace("planId", value.planId);
  result.emplace("planGeneration", static_cast<double>(value.planGeneration));
  result.emplace("streamId", value.streamId);
  result.emplace("streamGeneration", static_cast<double>(value.streamGeneration));
  result.emplace("mediaKind", value.mediaKind);
  result.emplace("state", value.state);
  result.emplace("producedUnits", static_cast<double>(value.producedUnits));
  result.emplace("deliveredUnits", static_cast<double>(value.deliveredUnits));
  result.emplace("presentedUnits", static_cast<double>(value.presentedUnits));
  result.emplace("muxedPackets", static_cast<double>(value.muxedPackets));
  result.emplace("committedBytes", static_cast<double>(value.committedBytes));
  result.emplace("deadlineMisses", static_cast<double>(value.deadlineMisses));
  result.emplace("droppedUnits", static_cast<double>(value.droppedUnits));
  result.emplace("counterEpoch", value.counterEpoch);
  return result;
}
struct ArtifactValidationResult {
  std::string observationId{};
  std::string processEpoch{};
  std::string authorityEpoch{};
  std::int64_t controlRevision{};
  std::string clockId{};
  std::int64_t clockGeneration{};
  std::int64_t observedAtNs{};
  std::string destinationId{};
  std::int64_t destinationGeneration{};
  std::string sessionId{};
  std::string artifactId{};
  std::int64_t artifactRevision{};
  std::string validatorId{};
  std::string validatorVersion{};
  std::string status{};
  std::int64_t checkedAtNs{};
  std::int64_t byteLength{};
  std::int64_t packetCount{};
  std::int64_t decodedUnits{};
  std::string checkSetId{};
  std::string resultId{};
};
inline bool validateArtifactValidationResult(const rpc::Json& value) {
  if (!value.isObject()) return false;
  const auto* observationId = value.get("observationId");
  if (!observationId || !(observationId->isString() && observationId->asString().size() >= 1)) return false;
  const auto* processEpoch = value.get("processEpoch");
  if (!processEpoch || !(processEpoch->isString() && processEpoch->asString().size() >= 1)) return false;
  const auto* authorityEpoch = value.get("authorityEpoch");
  if (!authorityEpoch || !(authorityEpoch->isString() && authorityEpoch->asString().size() >= 1)) return false;
  const auto* controlRevision = value.get("controlRevision");
  if (!controlRevision || !(controlRevision->isNumber() && std::floor(controlRevision->asNumber()) == controlRevision->asNumber() && controlRevision->asNumber() >= 0 && controlRevision->asNumber() <= 9007199254740991)) return false;
  const auto* clockId = value.get("clockId");
  if (!clockId || !(clockId->isString() && clockId->asString().size() >= 1)) return false;
  const auto* clockGeneration = value.get("clockGeneration");
  if (!clockGeneration || !(clockGeneration->isNumber() && std::floor(clockGeneration->asNumber()) == clockGeneration->asNumber() && clockGeneration->asNumber() >= 1 && clockGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* observedAtNs = value.get("observedAtNs");
  if (!observedAtNs || !(observedAtNs->isNumber() && std::floor(observedAtNs->asNumber()) == observedAtNs->asNumber() && observedAtNs->asNumber() >= 0 && observedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* destinationId = value.get("destinationId");
  if (!destinationId || !(destinationId->isString() && destinationId->asString().size() >= 1)) return false;
  const auto* destinationGeneration = value.get("destinationGeneration");
  if (!destinationGeneration || !(destinationGeneration->isNumber() && std::floor(destinationGeneration->asNumber()) == destinationGeneration->asNumber() && destinationGeneration->asNumber() >= 1 && destinationGeneration->asNumber() <= 9007199254740991)) return false;
  const auto* sessionId = value.get("sessionId");
  if (!sessionId || !(sessionId->isString() && sessionId->asString().size() >= 1)) return false;
  const auto* artifactId = value.get("artifactId");
  if (!artifactId || !(artifactId->isString() && artifactId->asString().size() >= 1)) return false;
  const auto* artifactRevision = value.get("artifactRevision");
  if (!artifactRevision || !(artifactRevision->isNumber() && std::floor(artifactRevision->asNumber()) == artifactRevision->asNumber() && artifactRevision->asNumber() >= 0 && artifactRevision->asNumber() <= 9007199254740991)) return false;
  const auto* validatorId = value.get("validatorId");
  if (!validatorId || !(validatorId->isString() && validatorId->asString().size() >= 1)) return false;
  const auto* validatorVersion = value.get("validatorVersion");
  if (!validatorVersion || !(validatorVersion->isString() && validatorVersion->asString().size() >= 1)) return false;
  const auto* status = value.get("status");
  if (!status || !(status->isString() && (status->asString() == "passed" || status->asString() == "failed" || status->asString() == "incomplete"))) return false;
  const auto* checkedAtNs = value.get("checkedAtNs");
  if (!checkedAtNs || !(checkedAtNs->isNumber() && std::floor(checkedAtNs->asNumber()) == checkedAtNs->asNumber() && checkedAtNs->asNumber() >= 0 && checkedAtNs->asNumber() <= 9007199254740991)) return false;
  const auto* byteLength = value.get("byteLength");
  if (!byteLength || !(byteLength->isNumber() && std::floor(byteLength->asNumber()) == byteLength->asNumber() && byteLength->asNumber() >= 0 && byteLength->asNumber() <= 9007199254740991)) return false;
  const auto* packetCount = value.get("packetCount");
  if (!packetCount || !(packetCount->isNumber() && std::floor(packetCount->asNumber()) == packetCount->asNumber() && packetCount->asNumber() >= 0 && packetCount->asNumber() <= 9007199254740991)) return false;
  const auto* decodedUnits = value.get("decodedUnits");
  if (!decodedUnits || !(decodedUnits->isNumber() && std::floor(decodedUnits->asNumber()) == decodedUnits->asNumber() && decodedUnits->asNumber() >= 0 && decodedUnits->asNumber() <= 9007199254740991)) return false;
  const auto* checkSetId = value.get("checkSetId");
  if (!checkSetId || !(checkSetId->isString() && checkSetId->asString().size() >= 1)) return false;
  const auto* resultId = value.get("resultId");
  if (!resultId || !(resultId->isString() && resultId->asString().size() >= 1)) return false;
  return true;
}
inline rpc::Json toJson(const ArtifactValidationResult& value) {
  rpc::Json::Object result;
  result.emplace("observationId", value.observationId);
  result.emplace("processEpoch", value.processEpoch);
  result.emplace("authorityEpoch", value.authorityEpoch);
  result.emplace("controlRevision", static_cast<double>(value.controlRevision));
  result.emplace("clockId", value.clockId);
  result.emplace("clockGeneration", static_cast<double>(value.clockGeneration));
  result.emplace("observedAtNs", static_cast<double>(value.observedAtNs));
  result.emplace("destinationId", value.destinationId);
  result.emplace("destinationGeneration", static_cast<double>(value.destinationGeneration));
  result.emplace("sessionId", value.sessionId);
  result.emplace("artifactId", value.artifactId);
  result.emplace("artifactRevision", static_cast<double>(value.artifactRevision));
  result.emplace("validatorId", value.validatorId);
  result.emplace("validatorVersion", value.validatorVersion);
  result.emplace("status", value.status);
  result.emplace("checkedAtNs", static_cast<double>(value.checkedAtNs));
  result.emplace("byteLength", static_cast<double>(value.byteLength));
  result.emplace("packetCount", static_cast<double>(value.packetCount));
  result.emplace("decodedUnits", static_cast<double>(value.decodedUnits));
  result.emplace("checkSetId", value.checkSetId);
  result.emplace("resultId", value.resultId);
  return result;
}
} // namespace corevideo::contracts
