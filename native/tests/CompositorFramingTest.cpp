#include "compositor/CompositorLayout.h"

#include <gtest/gtest.h>

// Placeholder — replaced by the compositor framing-math slice (fit/fill/
// stretch/zoom/offset/border source-rect tests).
TEST(CompositorFraming, Placeholder) {
  EXPECT_TRUE(true);
}
