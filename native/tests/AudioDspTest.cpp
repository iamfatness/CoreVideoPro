#include "modules/AudioDsp.h"

#include <gtest/gtest.h>

// Placeholder — replaced by the audio DSP-core slice (real RMS/peak/LUFS/
// limiter/bus-mix kernel tests).
TEST(AudioDsp, Placeholder) {
  EXPECT_TRUE(true);
}
