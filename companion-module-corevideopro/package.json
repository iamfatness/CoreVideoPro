{
  "name": "companion-module-corevideopro",
  "version": "0.1.0",
  "type": "module",
  "description": "Bitfocus Companion module for CoreVideo Pro — control transport, scenes, inputs, graphics, audio, multiview and automation over the app's HTTP/WebSocket control API.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@companion-module/base": "~1.11.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.12",
    "typescript": "^5.6.0"
  }
}
