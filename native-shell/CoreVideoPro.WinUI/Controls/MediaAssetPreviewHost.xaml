<UserControl
    x:Class="CoreVideoPro.WinUI.Controls.MediaAssetPreviewHost"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="using:CoreVideoPro.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Background="#050608">
        <MediaPlayerElement x:Name="VideoPreview"
                            AreTransportControlsEnabled="True"
                            Stretch="Uniform"
                            Visibility="Collapsed" />

        <Image x:Name="ImagePreview"
               Stretch="Uniform"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch"
               Visibility="Collapsed" />

        <StackPanel x:Name="EmptyState"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="6"
                    Margin="18">
            <TextBlock Text="{x:Bind EmptyTitle, Mode=OneWay}"
                       FontWeight="SemiBold"
                       FontSize="13"
                       Foreground="#E8F0EC"
                       TextAlignment="Center" />
            <TextBlock Text="{x:Bind EmptyDetail, Mode=OneWay}"
                       FontSize="11"
                       Foreground="{StaticResource StudioMutedBrush}"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       MaxWidth="320" />
        </StackPanel>
    </Grid>
</UserControl>
