<UserControl
    x:Class="CoreVideoPro.WinUI.Controls.MultiviewGridHost"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:cvcontrols="using:CoreVideoPro.WinUI.Controls"
    xmlns:models="using:CoreVideoPro.WinUI.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="mc"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    SizeChanged="OnSizeChanged">

    <Grid x:Name="GridSurface" Background="#020304" Padding="2">
        <ItemsRepeater x:Name="TileRepeater" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ItemsRepeater.Layout>
                <controls:UniformGridLayout x:Name="TileLayout"
                                            Orientation="Horizontal"
                                            MaximumRowsOrColumns="8"
                                            MinColumnSpacing="2"
                                            MinRowSpacing="2" />
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="models:ParticipantSurfaceTile">
                    <cvcontrols:BroadcastMultiviewTile Tile="{x:Bind}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
    </Grid>
</UserControl>